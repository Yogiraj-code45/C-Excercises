{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "(lldb) Attach",
            "type": "cppdbg",
            "request": "attach",
            "program": "enter program name, for example ${workspaceFolder}/a.out",
            "MIMode": "lldb"
        },
        {
            "name": "Debug C++ (Mac LLDB)",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/string",
            "args": [],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "Build C++ on macOS"
        }
    ]
}
